# Local Development Environment Variables

# API Proxy URL (required for Store/Find/Sync features)
VITE_API_PROXY_URL=http://localhost:8787

# API Key (required for authentication with worker)
# For local development, use any value (must match API_KEY in wrangler.dev)
# For production, generate a strong key: openssl rand -base64 32
VITE_API_KEY=dev-key-12345

# Copy this file to .env and update values for your environment
# DO NOT commit .env to git
